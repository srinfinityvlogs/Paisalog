{
  "name": "telegram-expense-bot",
  "version": "1.0.0",
  "private": true,
  "description": "Serverless Telegram bot that logs expenses (text + receipt OCR) to Google Sheets",
  "main": "api/webhook.js",
  "scripts": {
    "set-webhook": "node scripts/set-webhook.js",
    "delete-webhook": "node scripts/delete-webhook.js",
    "simulate": "node scripts/simulate-update.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "engines": {
    "node": ">=18"
  }
}
